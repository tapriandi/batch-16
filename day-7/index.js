// state : data
let global = "global";

// file coba
function coba(param) {
  let functionScope = "function Scope";
}

function baru(param) {
  let baru = "new ";
  console.log(baru + param);
}

// pemproses
coba(true);
baru(global);
